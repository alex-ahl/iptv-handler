version: "2"
syncs:
  server_sync:
    src: "./server"
    sync_excludes: [".git"]
    sync_strategy: "native_osx"
    sync_args:
      - "-logfile=/tmp/unison.log"

  db_sync:
    src: "~/docker-volumes/db"
    sync_strategy: "native_osx"
    sync_args:
      - "-logfile=/tmp/unison.log"
